pipeline {
  agent any
  triggers {
    GenericTrigger(
            token: 'chatroom'
    )
  }

  stages {

    stage('Build') {
      steps{
                  script{
                      println("获取代码")
                  }
           }
    }
  }
}
